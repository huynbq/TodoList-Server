NODE_ENV=development
PORT=3000
FRONTEND_ORIGIN=http://localhost:5173

# Runtime app connection. On Render, use Supabase session pooler or transaction pooler.
# If using transaction pooler on port 6543, append ?pgbouncer=true.
DATABASE_URL=""

# Migration connection. Use direct connection, Supavisor session pooler, or dedicated migration URL.
DIRECT_URL=""

# Supabase Auth. Use the publishable key when available, otherwise legacy anon key.
SUPABASE_URL="https://jhcleokmfkexdoyviyyx.supabase.co"
SUPABASE_ANON_KEY=""
SUPABASE_PUBLISHABLE_KEY=""
SUPABASE_SERVICE_ROLE_KEY=""
