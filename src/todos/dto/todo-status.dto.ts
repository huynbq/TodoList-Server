export enum TodoStatusDto {
  all = 'all',
  pending = 'pending',
  overdue = 'overdue',
  completed = 'completed',
}

export enum TodoWriteStatusDto {
  pending = 'pending',
  completed = 'completed',
}
